Includes

* Webpack
* React
* Stylus (by way of CSS Modules)

# To use
Best way to use this is to download the zip, unpack it and rename it. Then run `npm install`.
`npm run start` will kick off the webpack-dev-server.
